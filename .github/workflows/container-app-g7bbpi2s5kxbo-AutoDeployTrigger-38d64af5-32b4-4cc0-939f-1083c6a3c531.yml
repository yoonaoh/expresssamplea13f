name: Trigger auto deployment for container-app-g7bbpi2s5kxbo

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-g7bbpi2s5kxbo-AutoDeployTrigger-38d64af5-32b4-4cc0-939f-1083c6a3c531.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPG7BBPI2S5KXBO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPG7BBPI2S5KXBO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPG7BBPI2S5KXBO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPG7BBPI2S5KXBO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPG7BBPI2S5KXBO_REGISTRY_PASSWORD }}
          containerAppName: container-app-g7bbpi2s5kxbo
          resourceGroup: todonodejspgsqlgoogleauthaaa0bf31
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


